window.onload = function() {
    let penColor = 'black';

    function setPenColor(color) {
        penColor = color;
    }

    function setPixelColor(pixel) {
        pixel.style.backgroundColor = penColor;

    }

}

// add event listener (dica)
